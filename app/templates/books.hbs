<div class="row">
    <div class="col-sm-4">
        {{#link-to 'books.new' class="btn btn-success btn-block"}} ADD New book {{/link-to}}
        
        {{books-list books=books}}
    </div>
    <div class="col-sm-8">
        {{outlet}}
    </div>
</div>